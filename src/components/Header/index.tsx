import { PageHeader } from 'antd';

export const Header = () => (
  <>
    <PageHeader
      className="site-page-header-responsive"
      backIcon={false}
      title="Tasks"
      extra={[]}
    />
  </>
);