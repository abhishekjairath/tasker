import { Button } from 'antd';

export const AddTaskButton = () => {
    const addTask = () => {


    };

    return (
        <Button onClick={addTask} type="primary">
            Add Task
        </Button>
    );
};
